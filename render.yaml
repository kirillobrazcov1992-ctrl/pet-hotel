services:
  - type: web
    name: pet-hotel
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.cjs
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
